[package]
name = "custom-command"
version = "0.1.0"
edition = "2024"
authors = ["Pumpkin Developer"]
description = "A custom command plugin example for the Pumpkin Minecraft server"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
pumpkin-plugin-api = "0.1.0-dev"
tracing = "0.1"
